# Copy to .env.local and fill in. Vite inlines these at build time.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_URL=
# Optional: set to enable Cloudflare Turnstile on the login form. Leave empty
# and the widget is skipped entirely.
VITE_TURNSTILE_SITE_KEY=
